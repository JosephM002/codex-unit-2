const msg7 = document.getElementById("message");
if (msg7) {
  // TODO: insert explanatory text about API keys (do not include real keys)
  msg7.textContent =
    "An Api key is a string that APIs use as a code while communicating. They help id users and improve security of the site";
}
